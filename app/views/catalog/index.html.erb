<%= render :partial => 'phones' %>
<%= will_paginate @phones, :page_links => false, :link_separator => ' | ',
                          :previous_label => 'Página anterior', :next_label => 'Página siguiente' %>
